# 修改记录 — zhouli-translator / 黛玉腔坏字全量排查

> **修订记录**
>
> - v1.0 (2026-07-08): 续接 daiyu-tone 任务，全量排查并订正黛玉腔提示词中的编码坏字/错别字。

## 修改文件

### lib/prompt.ts — 黛玉腔视角指令块坏字订正（第 220–274 行）

- **修改位置**：`buildPerspectiveInstruction()` 内 `persona === "daiyu"` 的三处指令分支。
- **修改内容**（错别字 → 正字）：
  - `自嘐` → `自嘲`（5 处）
  - `戈然而止` → `戛然而止`
  - `哭哭啄啄` → `哭哭啼啼`（2 处）
  - `发氄` → `发飙`
  - `劝讫` → `劝诫`
  - `撞娇卖惨` → `撒娇卖惨`
  - `锋利但不撞泼` → `锋利但不撒泼`
- **说明**：以上均为昨日新增黛玉专属视角指令时混入的输入法坏字，仅影响提示词语义可读性，不改变控制流。

---

## 文件清单总览

| 操作 | 文件路径 |
| :--- | :--- |
| **修改** | lib/prompt.ts |
| **新增** | CHANGES.md |

---

## 测试方式

1. 全局扫描 U+FFFD 替换字符与坏字部首（颇/嘶/啄/氄/嘐/讄/顏）→ 全部清零。
2. `npm run typecheck` → 通过（tsc --noEmit 无报错）。
3. `npm run build` → 通过（Next.js 16.2.9，5/5 静态页生成成功）。
